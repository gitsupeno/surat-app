<x-app-layout><x-slot name="header"><div class="flex items-center justify-between"><h2 class="font-semibold text-xl text-gray-800">Detail Surat Keluar</h2><a href="{{ route('outgoing-letters.index') }}" class="text-sm text-indigo-600">Kembali</a></div></x-slot><div class="py-8"><div class="mx-auto max-w-4xl space-y-4 sm:px-6 lg:px-8">@if(session('status'))<div class="rounded-md bg-green-50 p-4 text-sm text-green-700">{{ session('status') }}</div>@endif<div class="rounded-lg bg-white p-6 shadow-sm"><dl class="grid gap-5 sm:grid-cols-2"><div><dt class="text-sm text-gray-500">Nomor surat</dt><dd class="mt-1 font-medium">{{ $outgoingLetter->letter_number }}</dd></div><div><dt class="text-sm text-gray-500">Status</dt><dd class="mt-1 font-medium">{{ ucfirst($outgoingLetter->status) }}</dd></div><div><dt class="text-sm text-gray-500">Tanggal</dt><dd class="mt-1 font-medium">{{ $outgoingLetter->date_letter?->format('d/m/Y') }}</dd></div><div><dt class="text-sm text-gray-500">Penerima</dt><dd class="mt-1 font-medium">{{ $outgoingLetter->recipient }}</dd></div><div><dt class="text-sm text-gray-500">Unit</dt><dd class="mt-1 font-medium">{{ $outgoingLetter->unit->name }}</dd></div><div><dt class="text-sm text-gray-500">Klasifikasi</dt><dd class="mt-1 font-medium">{{ $outgoingLetter->classification?->name ?: '-' }}</dd></div><div class="sm:col-span-2"><dt class="text-sm text-gray-500">Perihal</dt><dd class="mt-1 font-medium">{{ $outgoingLetter->subject }}</dd></div><div class="sm:col-span-2"><dt class="text-sm text-gray-500">Isi surat</dt><dd class="mt-1 whitespace-pre-line">{{ $outgoingLetter->body ?: '-' }}</dd></div></dl><div class="mt-6 flex gap-3">@if($outgoingLetter->status !== 'terkirim')<a href="{{ route('outgoing-letters.edit', $outgoingLetter) }}" class="rounded-md border border-gray-300 px-4 py-2 text-sm">Edit</a>@endif @can('approve outgoing_letter') @if($outgoingLetter->status === 'approval')<form method="POST" action="{{ route('outgoing-letters.approve', $outgoingLetter) }}">@csrf @method('PATCH')<x-primary-button>Setujui & Tandai Terkirim</x-primary-button></form>@endif @endcan</div></div></div></div></x-app-layout>
